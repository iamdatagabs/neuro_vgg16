- name: 📓 Validar notebook
  run: |
    NOTEBOOK="Experimento1_VGG16_Doc_Neuro.ipynb"

    python - <<EOF
    import nbformat, os

    path = os.environ.get("NOTEBOOK")
    assert os.path.exists(path), f"Notebook não encontrado: {path}"

    nb = nbformat.read(path, as_version=4)
    print("Notebook válido ✅")
    EOF
